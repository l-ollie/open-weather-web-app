import React from 'react';
import { Form } from 'react-bootstrap';

function Searcher(props: any) {
    return (
        <div>
            <Form.Control type="text" className="mt-4" />
        </div>
    );
}

export default Searcher;